* 对于最佳的存储，在所有情况下应尽量使用最精确的类型。例如，如果一个整数列用于范围从 1 到 99999 的值，MEDIUMINT UNSIGNED 是最适合的类型。表示所有需要的值的类型中，这个类型占用存储量最小。
* DECIMAL 列的所有的基础运算（加减乘除），65 位十进制精度都能搞定。
* 如果对精度要求不高或者对速度要求较高，DOUBLE 类型可能足够好。对于高精度的需求，可以转换为定点类型存储在 BIGINT 中。这使你可以使用 64 位整数执行所有计算，其次在必要时将结果转换为浮点值。
* PROCEDURE ANALYSE 可用于获取最佳列数据类型的建议。